name: frontend
description: Flutter frontend - Login/Cadastro conectado ao backend
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2  
  http: ^1.1.0
  flutter_secure_storage: ^8.0.0
  provider: ^6.0.0
  json_annotation: ^4.9.0  
  intl: ^0.18.0
  barcode_scan2: ^4.2.0

  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
